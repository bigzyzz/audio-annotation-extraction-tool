# Supabase (get from Supabase project settings -> API)
SUPABASE_URL=
# Service role key required: worker reads/writes jobs and storage across all users, RLS must be bypassable here.
SUPABASE_SERVICE_ROLE_KEY=

# How often the worker polls Postgres for pending jobs, in ms
JOB_POLL_INTERVAL_MS=2000
