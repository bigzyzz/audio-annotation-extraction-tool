# Supabase (get from Supabase project settings -> API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side only - never expose to the client. Used for privileged operations
# (e.g. admin tasks bypassing RLS). Do not prefix with NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=
