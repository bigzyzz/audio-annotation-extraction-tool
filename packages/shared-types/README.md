# packages/shared-types

Shared TypeScript types/interfaces used by both `apps/web` and `apps/worker` (e.g. DB row types generated from Supabase schema, job payload shapes, annotation record shape with `version` field for optimistic concurrency).

Not yet scaffolded.
